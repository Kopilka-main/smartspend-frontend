NUXT_AUTH_ORIGIN=
NUXT_PUBLIC_API_URL=

# Docker (docker-compose / run.sh): порт на хосте → контейнер :3000
APP_PORT=3000

# Опционально: другой compose-файл вместо docker-compose.yml
COMPOSE_FILE_NAME=docker-compose.yml